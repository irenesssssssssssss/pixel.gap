# Copy this file to .env.local and fill in your OpenAI API key.
# The server reads OPENAI_API_KEY only on the backend.
# For local development, you can place .env.local here in gap.game or one level up
# at the workspace root if that is where you already keep local secrets.

OPENAI_API_KEY=sk-...
